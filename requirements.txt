# --- Core data handling ---
numpy>=1.26
pandas>=2.2
scikit-learn>=1.4

# --- Deep learning (TCN model, CUDA-enabled) ---
torch>=2.2
torchmetrics>=1.3

# --- Explainability (Objective 3: resolving the Black Box problem) ---
shap>=0.45

# --- API layer for the clinical dashboard (Objective 4) ---
fastapi>=0.110
uvicorn[standard]>=0.29
pydantic>=2.6

# --- Config, reproducibility, utilities ---
pyyaml>=6.0
python-dotenv>=1.0
tqdm>=4.66

# --- Plotting (for paper figures) ---
matplotlib>=3.8
seaborn>=0.13

# --- Notebooks (EDA only — pipeline code lives in src/, not notebooks) ---
jupyter>=1.0
ipykernel>=6.29